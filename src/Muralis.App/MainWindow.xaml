<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="Muralis.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="Muralis"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Background="{ThemeResource MuralisAppBackgroundBrush}">
        <Grid.KeyboardAccelerators>
            <KeyboardAccelerator Key="D" Modifiers="Control,Shift" Invoked="OnDesignPlaygroundInvoked" />
            <KeyboardAccelerator Key="K" Modifiers="Control,Shift" Invoked="OnDockLabInvoked" />
        </Grid.KeyboardAccelerators>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar
            x:Name="AppTitleBar"
            Title="{Binding [App_Name], Source={StaticResource Loc}}"
            Subtitle="{Binding [App_Tagline], Source={StaticResource Loc}}"
            IsBackButtonVisible="False"
            IsPaneToggleButtonVisible="True"
            Foreground="{ThemeResource MuralisTextPrimaryBrush}"
            BackRequested="OnTitleBarBackRequested"
            PaneToggleRequested="OnTitleBarPaneToggleRequested">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="Assets/AppIcon.png" />
            </TitleBar.IconSource>
        </TitleBar>

        <NavigationView
            x:Name="RootNavigationView"
            Grid.Row="1"
            Background="Transparent"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            IsSettingsVisible="False"
            ItemInvoked="OnNavigationItemInvoked"
            OpenPaneLength="248"
            PaneDisplayMode="Left">
            <NavigationView.Resources>
                <SolidColorBrush x:Key="NavigationViewExpandedPaneBackground" Color="{ThemeResource MuralisSidebarColor}" />
                <SolidColorBrush x:Key="NavigationViewDefaultPaneBackground" Color="{ThemeResource MuralisSidebarColor}" />
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="{ThemeResource MuralisBackground0Color}" />
                <SolidColorBrush x:Key="NavigationViewItemForeground" Color="{ThemeResource MuralisTextSecondaryColor}" />
                <SolidColorBrush x:Key="NavigationViewItemForegroundPointerOver" Color="{ThemeResource MuralisTextPrimaryColor}" />
                <SolidColorBrush x:Key="NavigationViewItemForegroundPressed" Color="{ThemeResource MuralisTextPrimaryColor}" />
                <SolidColorBrush x:Key="NavigationViewItemForegroundSelected" Color="{ThemeResource MuralisTextPrimaryColor}" />
                <SolidColorBrush x:Key="NavigationViewItemForegroundSelectedPointerOver" Color="{ThemeResource MuralisTextPrimaryColor}" />
                <SolidColorBrush x:Key="NavigationViewItemForegroundSelectedPressed" Color="{ThemeResource MuralisTextPrimaryColor}" />
                <SolidColorBrush x:Key="NavigationViewItemBackground" Color="Transparent" />
                <SolidColorBrush x:Key="NavigationViewItemBackgroundPointerOver" Color="{ThemeResource MuralisSurfaceHoverColor}" />
                <SolidColorBrush x:Key="NavigationViewItemBackgroundPressed" Color="{ThemeResource MuralisSurfacePressedColor}" />
                <SolidColorBrush x:Key="NavigationViewItemBackgroundSelected" Color="{ThemeResource MuralisSurfaceSelectedColor}" />
                <SolidColorBrush x:Key="NavigationViewItemBackgroundSelectedPointerOver" Color="{ThemeResource MuralisSurfaceSelectedColor}" />
                <SolidColorBrush x:Key="NavigationViewItemBackgroundSelectedPressed" Color="{ThemeResource MuralisSurfaceSelectedColor}" />
                <SolidColorBrush x:Key="NavigationViewSelectionIndicatorForeground" Color="{ThemeResource MuralisAccentPrimaryColor}" />
            </NavigationView.Resources>
            <NavigationView.MenuItems>
                <NavigationViewItem Content="{Binding [Nav_Home], Source={StaticResource Loc}}" Tag="home">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE80F;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="{Binding [Nav_Browse], Source={StaticResource Loc}}" Tag="browse">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE774;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="{Binding [Nav_Library], Source={StaticResource Loc}}" Tag="library">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8F1;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="{Binding [Nav_Favorites], Source={StaticResource Loc}}" Tag="favorites">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE734;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="{Binding [Nav_Downloads], Source={StaticResource Loc}}" Tag="downloads">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE896;" />
                    </NavigationViewItem.Icon>
                    <NavigationViewItem.InfoBadge>
                        <InfoBadge x:Name="DownloadsBadge" Value="0" Visibility="Collapsed" />
                    </NavigationViewItem.InfoBadge>
                </NavigationViewItem>
                <NavigationViewItem Content="{Binding [Nav_Dynamic], Source={StaticResource Loc}}" Tag="dynamic">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE714;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="{Binding [Nav_Settings], Source={StaticResource Loc}}" Tag="settings">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE713;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <Frame x:Name="RootFrame" Background="{ThemeResource MuralisAppBackgroundBrush}" />
        </NavigationView>
    </Grid>
</Window>
